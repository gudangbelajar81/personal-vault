{
  "name": "personal-vault-monolith",
  "version": "1.0.0",
  "description": "Smart Personal Vault for managing passwords securely",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install-all": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  }
}
